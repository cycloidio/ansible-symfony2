---
- name: Update current Symlink
  sudo: yes
  sudo_user: '{{ deploy_user }}'
  file: state=link 
        path={{ current_path }}
        src={{ release_path }}
  notify:
    - restart php-fpm

- meta: flush_handlers
